# AVA Roadmap

- Put TLS reverse proxy in front of NodeBackend.
- Add production-grade AVA_stock persistence behind the existing `/api/ava-stock/**` contract.
- Connect AVA AI gateway responses to the local LLM planner for richer tool execution.
- Add automated end-to-end smoke tests for Windows desktop, Android, macOS, and iOS release candidates.
